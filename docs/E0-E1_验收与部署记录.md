# E0＋E1 验收与部署记录

日期：2026-09-13。范围：内部邀请访问、真实 PostgreSQL 持久化的普通旅行闭环。执行：程序员 Astra 轻；独立复核：开发策略任务。所有输入和参与者均为合成研发样本。

## 状态

- E0＋E1 本地工程验收通过，应用代码已冻结；未进入 E2。
- 代码基于已有 GitHub 仓库 `zyrobbie/travel-cat-planner` 的 `main`，保留既有策划站点与提交历史。
- 后端工程初始提交为 `59c0c17`；最终应用提交 `288f71225c4a48139af43f30bed1429710deed0e` 已推送 `main` 与 `codex/e0-e1`。
- 用户指定只使用 GitHub Pages，静态体验版已实际发布并完成线上核对，数据仅保存于当前浏览器；下述数据库、会话及权限结论均对应保留的 Node 后端版本，不可套用到静态版。

## 实际发布结果

- [正式体验地址](https://zyrobbie.github.io/travel-cat-planner/app/)；[首页与历史文档目录](https://zyrobbie.github.io/travel-cat-planner/)。未使用 Netlify，也未创建云数据库。
- [Pages 测试与发布 34749151212](https://github.com/zyrobbie/travel-cat-planner/actions/runs/34749151212) 全部成功，包含干净安装、内容校验、静态构建、3 组浏览器测试、打包和部署。
- [保留后端 CI 34749151197](https://github.com/zyrobbie/travel-cat-planner/actions/runs/34749151197) 全部成功，包含真实 PostgreSQL 迁移／测试、Next 构建和端到端测试。
- 两条 CI 均对应上述完整应用提交。随后补充的发布记录只修改文档，不改变已经上线的应用产物。

统筹使用内置浏览器直接访问正式 HTTPS 地址，以合成“线上验收猫”执行命名→投递需求卡→顶栏返回立即显示→回应仅系统确认→刷新→独立旅行→普通萤火虫明信片→回家→从裸链接继续原体验→回看原回应。各步均在真实线上页面完成；读取到的脚本为 `app/assets/index-DJLlvRK6.js`，与构建产物一致。用户页控制台 error/warn 为空；390px 正文截图保存在 `/tmp/catletters-root-review/live-mobile-postcard.png`。原首页已显示体验入口及历史文档标记。

线上人工走查与本地／CI 的故障注入分开记录：并发、存储故障等完整负例由上述自动回归验证；没有声称在真实手机、Safari、微信或多设备之间完成验收。

## Pages 静态版验收

代码冻结后，统筹实跑 `npm run pages:build`、`node scripts/package-pages.mjs` 和 `npm run pages:test`，构建及 3/3 浏览器测试通过。最终应用资源为 `index-DJLlvRK6.js`、`index-DI-DoRUE.css`。发布包只有应用静态资源、首页、原文档阅读器、原决策页与两份已有公开文档。

独立内置浏览器实跑：直接命名“Pages小橘” → 零回应独立旅行 → 完整萤火虫 ordinary → 从裸 `app/` 链接继续原体验 → 回家 → 日常卡回应仅系统确认 → 刷新后保持在家及历史。另检查打包后的根目录首页、390px 入口与 `app/` 链接，未出现资源加载或控制台错误。

自动回归覆盖 T1/T2/T3/T7、刷新与裸链接恢复、同信并发、双页过期控制操作、存储不可用及写入失败、本机参与者分区、延迟读取切换、发送期间切换保护，以及控制台投递后顶栏返回立即更新。独立审计的两项切换竞态已修复并有对应回归。详见 `docs/Pages_开发验证记录.md`。

统筹实际查看最终手机完整明信片 PNG，确认正文、署名、收好按钮无遮挡；执行者还检查了 P1/P3、桌面明信片及写失败截图。记录位于 `/tmp/catletters-pages-evidence/`。没有把桌面模拟视口当成真实手机、Safari 或微信浏览器验收。

## 独立页面验收

使用本地生产构建 `http://127.0.0.1:3100`，Codex 内置浏览器，桌面 1280×900、手机视口 390×844；没有使用真实手机、微信内置浏览器或跨设备同步验收。

| 检查 | 实际结果 |
|---|---|
| 命名与在家空状态 | Admin 建立新匿名参与者；用户兑换邀请、命名为“验收小橘”，显示在家及无新来信 |
| T1 回应确认 | 实际提交合成回应，只显示“送出去啦。”，返回后仍在家 |
| T2 同日两次回应 | 连续投递并回应两张需求卡，刷新后仍在家，没有旅行或明信片 |
| T7 跳过 | 第三张需求卡选择“这次先不回”，没有催促；随后可独立开始旅行 |
| 普通旅行闭环 | Admin 开始旅行、投递萤火虫 ordinary；用户打开全文、回看历史；Admin 结束旅行后用户回到在家状态 |
| 刷新持久化 | 刷新后旅行状态存在；来信盒可重新打开明信片及此前实际输入的回应 |
| 真实页面可读性 | 检查手机首屏及滚动后的末段、署名、按钮；最长 ordinary 全文可读；390px 无横向溢出；桌面历史回应可读 |
| 控制台 | 本次独立用户页流程读取到的 error/warn 为空 |

独立截图为本机临时验收产物：`/tmp/catletters-root-review/mobile-postcard.png`、`/tmp/catletters-root-review/desktop-postcard.png`、`/tmp/catletters-root-review/desktop-history.png`。前两张在详情页移除固定底栏后重新获取并查看，正文末段、署名和按钮无遮挡。截图不包含密钥，不作为正式视觉设计验收。

## 内容复核

独立从原始规格的明确行范围逐字比对 6 demand、3 linked、3 ordinary、6 tips，共 18 段主正文，均一致。仅移除 Markdown 引用符及格式换行空格；未将“假设用户回应”预填为用户历史。

发现并修复了 W1-G “不设置”下负例被作为系统词条导入的问题。修复后为 53 个合法系统引用块，正文一致。三组 linked／ordinary 共享场景元数据，季节与时段分开；linked 在本阶段仅导入内容，未实现历史联动。

## 可靠性审查

以下问题已修复，并以源码复核和针对性回归核实：

1. 初次读取的服务故障被误当成未登录：区分 401 与服务不可用。
2. 已落库但响应丢失后修改文字，再试会假报成功：仅对不确定传输故障查询结果，并核验同来信和正文摘要。
3. 一次性邀请响应丢失无法恢复：Admin 为同一参与者重发邀请、撤销旧会话，保留历史。
4. Admin 重试可能多投递下一张卡：服务端保存幂等操作结果；界面“写入成功、刷新失败”的重试继续使用同一请求键，端到端回归通过。
5. 本机启动脚本验证健康检查后才报告启动成功，重复启动保留有效进程信息。

最终检查：执行者的 `npm run build`、`npm run test:e2e`（2/2）及应用＋PostgreSQL 实际重启恢复通过，统筹读取了对应日志。代码冻结后，统筹另行实跑 `npm run content:check`、`npm run typecheck` 和 `npm test`（3/3）通过；其中真实数据库测试包括 T1/T2/T3/T7、8 次并发发送和参与者隔离。内容检查的首次运行受本机沙盒 IPC 限制，获准后用同一命令成功执行，未把环境错误计为业务测试失败或跳过。

## 部署边界

用户在本轮明确选择只用 GitHub Pages，不使用 Netlify。现改为发布独立静态体验版，数据只在当前浏览器保存；Node＋PostgreSQL 工程保留，可本地运行，本轮不部署到云端。不存在待用户选择其他托管平台的前置条件。

静态版构建产物位于 `dist/pages/app`；发布打包脚本仅收集该应用与已有公开策划页到 `dist/github-pages`，不上传服务器配置、数据库或会话。原站点保留于根目录，应用入口为 `/travel-cat-planner/app/`。GitHub Pages 工作流构建和发布后，必须检查真实线上命名、写入、刷新和旅行流程。

后端 Dockerfile 本轮本机未运行 Docker 构建。静态版上线不代表全栈后端已经上线，也不代表本机演示分区具有服务端安全隔离。

本轮不包含 E2 的 linked 投递、来源更正删除、暂停恢复、自留句和 REALTIME worker。输入检查为明确标注的内部合成适配器，未验证真实风险识别能力。
